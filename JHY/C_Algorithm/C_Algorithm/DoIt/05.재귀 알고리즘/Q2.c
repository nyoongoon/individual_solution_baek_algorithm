// 재귀 함수 호출을 사용하지 않고 gcd 함수를 작성하시오

/*
 1. 두 수를 받음 n, m
 2. n > m 이면 n % m 을 한 후 나머지를 다시 m으로 나누고 이런식으로 반복... 0이 나오면 그 수가 gcd!
 */

#include <stdio.h>

int gcd(int n, int m);

int main(void)
{
    int n, m, mod = 0;
    printf("두 수를 입력하세요! ");
    scanf("%d %d", &n, &m);
    while (1) {
    }
    return 0;
}

int gcd(n ,m)
{
    if(m = 0)
        return n;
    else
    {
        
    }
    return 0;
}
