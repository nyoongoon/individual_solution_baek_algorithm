#include <stdio.h>

int main(void)
{
    int N,M;
    scanf("%d %d", &N, &M);
    for(int i = 2; i < N; i++)
    {
        for(int l = 1; l < i; l++)
        {
            if(i % l != 0)
        }
    }
    return 0;
}
